package dev.webbies.dotenvify.settings

/** Single source of truth for the format-option checkbox labels (panel + both settings pages). */
object FormatOptionLabels {
    const val EXPORT = "Add export prefix"
    const val SORT = "Sort alphabetically"
    const val SKIP_LOWERCASE = "Skip lowercase keys"
    const val URL_ONLY = "Keep only URL values"
}
