// Fired by CTAs (e.g. "try it live") to pre-fill the in-browser demo with the
// sample input and draw the eye to the formatted output. LiveDemo listens for it.
export const LOAD_EXAMPLE_EVENT = "dotenvify:load-example";
